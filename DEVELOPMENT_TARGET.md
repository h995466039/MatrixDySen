# 星环回声：持续开发目标

## 目标

把当前的工厂 UI/UX MVP 推进为一个有明确目标、可观察、可扩张的浏览器工厂游戏。第一阶段不追求完整复刻大型游戏的全部系统，而是先完成一条能让玩家自然循环 20-40 分钟的纵向切片：

`采矿 -> 加工 -> 供电 -> 矩阵科研 -> 解锁 -> 星际货运 -> 异星资源 -> 更高阶科研`

玩家始终应该能回答：

1. 下一步要做什么？
2. 当前瓶颈是资源、产线、能源还是科技？
3. 我能通过改产线、换能源或开新星球解决它吗？

## 核心玩法重审

### 地表工厂

- 采矿机覆盖矿脉，产出原矿缓存。
- 传送带是实际物流，不是装饰线；输入端、输出端和方向决定产线是否运行。
- 冶炼机把铁、铜、硅变成金属锭和硅片。
- 组装机和工作台生产处理器。
- 科研站生产四类矩阵，产物必须通过出料分拣器进入物流仓储后才会被研究循环消费。
- 电网同时显示发电量、耗电量和净余量；火电机缺煤时会停机。

### 科研层

- 主线：行星物流 -> 自动冶炼 -> 矩阵实验室 -> 星际物流 -> 恒星网络 -> 戴森框架。
- 分支：智能分拣、高速传送、风能捕获、热能转化、石化开采、精密工作台、高级组装、高压采掘、电网增容、深层泵压。
- 科研矩阵是“反向加速科技”的唯一推进资源，玩家必须先解决生产，再获得解锁。
- 建筑不是永久免费权限：每座建筑都绑定解锁科技；基础工业由初始“基础工业授权”发放，高级设施由主线或分支研究解锁。
- 科技既能解锁建筑，也能升级已有建筑：高速传送减少物流时间，自动冶炼减少冶炼时间，高级组装减少组装时间，高压采掘和深层泵压提升采集速度，矩阵实验室提升科研站制备速度，电网增容提升发电设施输出。

### 跨星际层

- 星际物流完成后开启星图，不把星图做成独立的静态菜单。
- 母星是发射点，熔火-β提供钛，霜环-7提供冰，档案星提供信息矩阵。
- 一次航次有明确的装载成本、去程、抵达、返航和回收结果。
- 航线在星图关闭时仍推进，回到星图可以看到飞船位置、阶段和剩余时间。
- 异星资源回到物流仓储，直接参与后续生产和研究；风力、火力等发电设施只提供电力，不接受物流物料。
- 每个星球都有科技门槛和明确收益，避免“点一下就全解锁”。

## 玩家引导

目标面板采用阶段任务而不是长篇说明：

1. 采矿：部署采矿机。
2. 加工：用传送带连接冶炼机。
3. 科研：部署科研站，生产矩阵并启动科技。
4. 星际：完成主线科技，选择星球并派遣第一艘货运舱。
5. 恒星工程：用异星资源持续推进恒星网络和戴森框架。

每个阶段都提供一个动作按钮，直接选择建造工具、打开科技中枢或打开星图。提示文案必须解释“下一步动作”和“完成后的反馈”，不只显示一个百分比。

## UI/UX方向

- 地表画布始终是主界面，工业信息覆盖在画布边缘，不遮住产线。
- 科技中枢负责“选择研究目标”和“看矩阵库存”。
- 星图负责“选星球、选货物、看航线、看航次记录”。
- HUD 使用同一套信息语法：名称、当前状态、进度条、可执行动作。
- 锁定内容仍可查看，但必须显示锁定原因、前置科技和解锁收益。
- 所有会改变状态的按钮都需要禁用态、库存不足态、运行中态和完成态。
- 桌面 1280px 是主体验尺寸；移动端只做结构性收缩，不追求完整移动端操作。

## 动画目标

- 传送带：方向箭头、流动光点和真实物品沿线移动。
- 建筑：运行时呼吸缩放、能量弧线、工厂与发电设施之间的脉冲信号。
- 科研：矩阵库存变化、研究进度和状态文案同步变化。
- 星图：恒星脉冲、轨道层、航线虚线、货运舱去程/返航、航次完成日志。
- 动效服务于状态表达，不能让玩家等待；支持 `prefers-reduced-motion`。

## 素材计划

生图素材统一放在 `output/imagegen/`，命名采用功能语义和版本号：

- `building-*`：科研站、风力发电机、火力发电机、石油提取机、分拣器。
- `resource-*`：四类科研矩阵、钛矿、异星样本。
- `planet-*`：母星、熔火-β、霜环-7、档案星。
- `space-*`：货运舱、轨道信标、航线特效素材。

每份素材接入前要检查主体是否居中、轮廓是否清晰、背景/透明度是否符合消费方式、是否有文字或水印。生成服务不可用时，游戏必须保留程序绘制 fallback；不能因为图片失败出现空白建筑或断裂 UI。

## 本轮验收

- 新存档可以从地表工厂进入科研循环。
- 旧存档缺少跨星际字段时可以正常加载。
- `M` 或顶部“星图”可以打开星图；未完成科技时能看到锁定原因。
- 完成“星际物流”后，选择远端星球和货物可以发射货运舱。
- 货运舱去程、返航和回收结果可观察，关闭星图后仍继续推进。
- 刷新页面后科技、航线、航次记录和库存仍然存在。
- 本地和公网路由都能加载脚本及图片资源。

## Sprint 2：可玩性与反馈强化

本轮把纵向切片从“能放置建筑”推进到“能理解、能诊断、能完成一段主线”：

- 职业选择不再只是头像标签。物流主管、能源工程师、科研先驱分别改变传送带速度、发电量和科研推进效率，并写入本地存档。
- 工厂脉冲监视器持续报告运行设施、运输中的物料、已完成航次，并把当前最值得处理的瓶颈直接转成一句行动建议。
- 选中建筑后显示真实状态、配方、缓存输出、供电类型和当前进度，状态包括缺料、堵塞、缺煤、断电和生产中。
- 机器使用已有生图建筑贴图作为主体，叠加转子、火焰、机械臂、实验室轨道、风机叶片和抽油泵运动，形成可持续运行的动画帧效果。
- 星图航线加入访问次数、去程/返航状态、后台推进和资源回收后的日志反馈。
- 戴森框架研究完成后新增恒星工程施工台：每次消耗结构矩阵、钛和处理器部署一个组件，进度同步到星图与地表轨道。

### Sprint 2 验收清单

- [x] 旧存档可加载，缺少职业、访问记录和恒星工程字段时自动补齐。
- [x] 新存档先选择职业，确认后职业锁定并保存。
- [x] 选中任意建筑可看到配方和状态，不需要打开额外设置页。
- [x] 电力不足、生产缺料和输出堵塞会在工厂监视器中给出具体建议。
- [x] 主线完成「戴森框架」后，玩家可以用异星资源部署恒星轨道组件。
- [x] `prefers-reduced-motion` 下禁用持续旋转、脉冲和进度过渡。
- [ ] 生图服务恢复余额后补齐缺失的矩阵、行星和高级建筑贴图，并逐张进行透明度与构图检查。

## Sprint 3：建筑科技与升级闭环

本轮把“研究完成”变成可观察的工厂能力变化，确保科技不是只更新一张卡片：

- [x] 所有建筑数据都具备科技门槛；基础设施统一绑定“基础工业授权”，高级设施绑定对应分支科技。
- [x] 科技节点区分“建筑解锁”和“建筑升级”，卡片显示实际作用与目标建筑。
- [x] 采矿、冶炼、组装、科研制备、传送带和发电计算统一读取已研究升级。
- [x] 建筑检查器显示解锁科技、已研究升级和下一项升级；升级建筑在地表显示 Mk 标记。
- [x] 旧存档自动补齐基础工业授权，避免历史工厂因为新增门槛无法继续运行。
- [x] 基础授权的解锁清单与实际建筑门槛保持一致；高级组装同时作用于组装机和工作台。
- [x] 智能分拣研究后，分拣器具备真实输入、转运节拍和输出物流，不再只是静态解锁。
- [x] 增加浏览器运行态 QA 演示链路，验证采矿脉冲、研究消耗、建筑门槛、分支升级、星际去返、异星回收和恒星工程 100%。
- [x] QA 覆盖全部建筑科技分支，并断言真实科研站矩阵产出、研究消费、采矿、石油提取、冶炼、组装、物流和发电升级会改变实际运行参数。
- [x] QA 进一步验证新存档的矿机→冶炼机真实物流推进，而不是只验证矿脉数值变化。
- [x] 科研站输入和矩阵回流改为强制走传送带；物流仓储出料会按目标配方缺口轮换物料，避免科研链被单一资源霸占。
- [x] QA 演示与普通玩家存档隔离，带演示参数的浏览器运行不会覆盖本地工厂进度。
- [ ] 在同一浏览器页签完成从基础工业授权到至少一项分支升级、星际物流和恒星工程的完整试玩回归。

## Sprint 4：科技授权运行时闭环

本轮把建筑科技从“建造入口限制”推进为贯穿工厂模拟的运行规则，避免旧存档或调试状态绕过科技后产生不一致：

- [x] 未解锁建筑在运行时保持停机状态，不计入电网负载与发电量，不接收物流，也不推进生产进度。
- [x] 建筑被选中时显示“科技锁定”、所需授权和当前许可等级；科技完成后自动恢复为可运行状态。
- [x] 建造栏、放置校验、物流投递、生产模拟、动画状态与建筑检查器统一读取同一个科技许可判定。
- [x] 旧存档中的未知建筑记录会被安全忽略，已存在但尚未授权的已建建筑保留并等待对应科技，不破坏存档结构。
- [x] QA 增加未授权建筑停机、电网排除、授权后恢复生产和解锁后可放置断言。

## Sprint 5：分拣器分流与转向物流

本轮把智能分拣从“可建造建筑”推进为可配置的物流节点，让传送带布局开始具备自由组合和分支生产的意义：

- [x] 分拣器正确接收输入、按节拍处理并从输出侧传送带发出物料。
- [x] 选中分拣器后可为不同物料指定出口；未指定时根据下游配方自动寻找可用路线。
- [x] 分拣器出口由实际相邻传送带识别，支持两条及以上输出路线，并在检查器显示出口方向。
- [x] 多段直线与转角传送带通过连接拓扑连续运输，分叉时优先选择能接收当前物料的路线。
- [x] 移除传送带后自动清理分拣器的失效规则，避免存档留下不可见出口。
- [x] 旧存档即使残留已删除传送带的出口 ID，也会自动清除规则并恢复可用路线。
- [x] QA 增加“铜指定出口、铁自动出口、转角续接”的真实运行断言，并增加 `?demo=sorter` 视觉验收态。

## 后续阶段

本轮之后的开发顺序：

1. 增加矿脉勘探、星球表面资源差异和航线容量升级。
2. 增加恒星网络的轨道中继建筑与戴森框架可视化施工。
3. 生图服务恢复后，补齐当前预留的建筑、星球、矩阵和货运舱素材，并逐张替换 fallback。

## Sprint 6：素材运行时韧性与验收

本轮补充了素材加载的运行时保护，确保单张 PNG 生成失败或静态资源暂时不可用时，游戏仍保持可玩状态：

- [x] 科研矩阵图标加载失败时回退到带颜色语义的几何矩阵图标。
- [x] 建造栏图片加载失败时隐藏破图并显示对应设施符号；Canvas 继续使用建筑专属机械轮廓和动画。
- [x] 货运舱图片加载失败时保留方向、颜色和悬浮动画，不阻断星图操作。
- [x] 资源加载路径纳入本地 HTTP 验收；已有素材必须返回 200，预留素材明确走回退，避免意外破图进入部署。
- [ ] 生图服务恢复后补齐 10 个缺失 PNG：4 种科研矩阵、5 种科技建筑和 1 艘货运舱，并逐张检查 Alpha、主体完整度和小尺寸可读性。

## Release gate：2026-09-19

- [x] 本地 `?qa=playthrough` 浏览器运行态回归通过：采矿→冶炼→矩阵生产、建筑科技授权/升级、分拣分流、星际去返和恒星工程 100%。
- [x] 本地脚本语法检查通过，核心页面、脚本、样式和已有 PNG 均通过 HTTP 200 检查。
- [x] 公网 `/daisen/` 已部署当前版本 34；远端核心文件 SHA-256 与本地一致，并保留 `/var/www/html/daisen.bak-20260919-34` 备份。
- [x] 科研站输入与矩阵回流必须经过传送带和分拣器，物流仓储按建筑配方缺口派料；没有运输路径时不会伪造科研进度。
- [ ] 使用用户当前浏览器页签完成一次不新开页签的人工试玩回归；当前 CUA 会话未暴露已有页签，因此不能伪造该项结果。

## Sprint 7：星球资源与规模化物流

下一阶段目标是让“解锁建筑”继续转化为可规划的星际工厂能力：

- [ ] 为每个星球加入资源分布与矿脉勘探结果，航线不再只返回固定奖励。
- [ ] 增加轨道中继建筑科技，跨星球运输容量、返航效率和可用航线由科技升级驱动。
- [ ] 将恒星网络施工拆成可见的轨道中继与戴森框架组件，施工材料消耗与星图进度保持同步。
- [ ] 补齐生图服务恢复后的 10 个预留贴图，并增加透明度、主体居中、小尺寸可读性检查。
- [ ] 完成同一浏览器页签人工回归后，再将本节首项目标拆为可独立验收的 QA 场景。

## Sprint 8：仓储与分拣器物流闭环

本轮把物流规则收敛成可验证的工厂基础设施：仓储保存物料，传送带只运输，分拣器负责建筑接口。

- [x] 发电设施只提供电力，不保存物流物料，也不接受建筑直连投料。
- [x] 物流仓储拥有真实库存、容量、入库分拣器和出库分拣器。
- [x] 普通建筑的输入与输出都必须通过相邻分拣器，传送带不能直接把物料交给建筑。
- [x] 分拣器支持“传送带 → 建筑”和“建筑 → 传送带”两种方向，并在检查器显示连接状态。
- [x] 仓储 Mk-II 通过科技提升容量；行星物流站作为跨星际仓储节点解锁。
- [x] 科研、冶炼、组装、采矿、星际货运和恒星工程统一从物流仓储读写物料。
- [x] QA 覆盖建筑直连禁止、矿机→冶炼机、仓储进出、科研消费、分拣规则分流、星际去返和恒星工程。
- [ ] 增加独立的物流诊断层：显示每条分拣器两侧端口、阻塞原因和预计吞吐。

## Sprint 9：多级物流与规模化生产

- [ ] 增加物流塔/星际物流站的多级接口：行星内运输、星际运输和仓储容量分别可升级。
- [ ] 为传送带、分拣器、仓储和物流站增加 Mk-I / Mk-II / Mk-III 的真实吞吐、功耗与材料差异。
- [ ] 增加多层生产蓝图：同一建筑可通过科技升级配方槽、并行产线数量或输入缓存，而不是只显示等级徽标。
- [ ] 增加资源优先级、分拣器过滤规则和仓储最小库存策略，支持玩家搭建可控的分流网络。
- [ ] 将星球资源、航线容量、物流站装卸速率与主线科技和分支科技绑定，形成跨星际扩张的长期目标。

## Sprint 10：启动闸门与分拣器安装体验

本轮把进入游戏和物流接口建造改成可观察、可学习的操作流程：

- [x] 页面启动时统一预加载脚本图片、界面图片、职业头像和星图背景；每项资源都在 `load/error` settle 后推进进度。
- [x] 加载错误不会阻塞进入游戏，缺失素材会记录到 `data-asset-failures` 并切换到程序绘制 fallback。
- [x] 游戏初始化、QA 演示、HUD 更新和主循环延后到资源队列完成后执行。
- [x] 分拣器不再沿用普通建筑的空地点击放置；玩家从建筑接口或传送带端点开始拖拽。
- [x] 建筑 → 传送带自动识别传送带起点并创建出料分拣器；传送带末端 → 建筑自动创建进料分拣器。
- [x] 分拣器预览显示建筑接口、传送带端点、连接线和自动吸附的幽灵位置，并在无合法位置时明确说明原因。
- [x] 安装时继续使用科技许可、建材消耗、仓储/建筑物流契约和既有分拣器方向逻辑。
- [x] QA 覆盖出料/进料吸附坐标、真实安装生成、资源加载完成标记和原有全流程回归。

## Sprint 11：浏览器素材接入与新手恢复

本轮目标是让现有素材真正服务浏览器主线，并让玩家在生产链卡住时能继续定位下一步：

- [x] 使用 Godot 已生成的透明建筑贴图作为浏览器缺失建筑的运行时降级来源，科研站、分拣器、风力发电机、火力发电机和石油提取机不再请求不存在的 v01 PNG。
- [x] 将物流仓储贴图接入浏览器 Canvas，将平原、岩场和水域瓦片接入地表背景；工业地板瓦片保留为未应用备用素材。
- [x] 电磁矩阵库存使用现有矩阵贴图；能量、结构和信息矩阵保留颜色/几何 fallback，避免使用同一张错误图标混淆语义。
- [x] 货运舱和三种高级矩阵不再作为破图直接加载，缺失状态通过稳定 fallback 表达。
- [x] 试玩确认科技面板、职业面板、星图入口和科研缺料诊断在已有存档中可打开并持续更新。
- [ ] 通过真实新存档完成一次“职业选择 → 采矿 → 分拣器物流 → 科研站”的手动试玩回归。
- [ ] 补齐货运舱、科研站/分拣器独立 v01 贴图和三种高级矩阵图标，并进行透明度与小尺寸可读性检查。

## Sprint 12：统一瓶颈诊断与回归闭环

本轮目标是让玩家从任意一个状态入口都能得到同一条可执行建议，并保证自动化回归不会改写试玩中的最终状态：

- [x] 用统一的工厂诊断对象计算阶段进度、异常原因、建议动作和聚焦目标。
- [x] 主目标、Factory Monitor 和科技中枢共享同一条诊断文本与动作按钮。
- [x] 加工阶段按“矿机出料分拣器 → 冶炼机进料分拣器”的真实拓扑判定，避免仅有建筑或孤立传送带时误报完成。
- [x] 诊断动作先选工具再聚焦目标建筑，保留可直接继续建造的上下文。
- [x] QA 分流拓扑测试结束后恢复完整主流程状态，并断言完成态目标进入“第一圈戴森框架已点亮”。
- [ ] 在不触碰用户现有存档的前提下，完成一次独立新存档的职业选择、采矿、分拣器物流和科研站手动回归。

## Sprint 13：地形瓦片与建筑体积碰撞

本轮目标是让地表从“只有网格的背景”变成可读、可规划、可约束的建造空间：

- [x] 增加固定且可复现的平原、玄武岩岩场和低洼水域瓦片，不增加存档字段，不改变已有工厂位置。
- [x] 建筑放置按完整 footprint 逐格检查世界边界、已有建筑、传送带和禁建地形，旋转不再绕过占地碰撞。
- [x] 传送带寻路、传送带直铺和分拣器候选位置统一避开岩石、水域与边界。
- [x] 建造预览显示每个占地格、碰撞交叉标记、地形禁建色和可执行的失败原因；鼠标读数同步显示阻挡 footprint 的地形。
- [x] 增加地表状态图例，调整 Factory Monitor、选中检查器和建造栏的垂直层级，减少中等高度画面的信息遮挡。
- [x] QA 覆盖地形生成、建筑/传送带 footprint 碰撞、禁建区域和原有采矿→科研→星际→恒星工程流程。

## Sprint 14：生存开局与应急制造

本轮把开局从“库存直接购买建筑”改成有资源压力但不阻塞新手的生存式起步：

- [x] 移除能源核心建筑和相关物料接口；初始电力来自着陆舱基础电源，后续由风力、火力等发电设施扩容。
- [x] 新存档提供少量采矿机、熔炼炉、分拣器和传送带套件，保证玩家可以立即搭建第一条采矿物流线。
- [x] 增加“应急制造”面板，玩家可以用随身铁、铜手搓基础建筑套件和传送带组件。
- [x] 建筑放置优先消耗对应套件；套件耗尽后才回退到原料建造，拆除时按来源回收。
- [x] 旧存档自动补齐套件字段，不改写已有建筑、库存、科技和物流进度。
- [ ] 将基础手搓配方扩展为可配置的便携制造队列，并让更高级建筑需要处理器、矩阵和异星材料组装。

## Sprint 15：分段零件闭环 + QA 回绿
- [x] 组装机/工作台接入 10 种分段零件配方（齿轮/磁线圈/电路板/玻璃/电动机/涡轮机/粒子容器/太阳帆/结构火箭/轨道节点），按建筑分型。
- [x] 检查器内点击循环切换配方，科技门槛锁定时机器停机且拒绝输入并显示「配方锁定」。
- [x] 仓储派料、分拣器路由、诊断建议、建造栏接入新零件与两个发射台（orbital-construction 门槛 + hotkey）。
- [x] 戴森组件模型：轨道节点 20 座定进度，太阳帆/结构火箭带存续时间（脱落需补发），恒星接收器需节点满 + 帆/火箭齐备；旧档 modules 自动迁移为节点组件。
- [x] 星图展示组件构成（节点/帆/火箭）与缺失/脱落警告。
- [x] `node qa-harness.cjs playthrough` 回绿（新增 9 条断言）。

## Sprint 16：交互 parity（对标戴森球计划）
- [x] 时间控制：速度 1/2/4/10x，`=`/`-` 热键调速，暂停标识。
- [x] 小地图（右上）：矿脉/建筑/传送带/视口矩形，点击拖拽跳转，4Hz 节流，jsdom 容错。
- [x] 飞行定位 flyToCell（平滑插值、reduced-motion 瞬移）+ 选中卡「◎定位」按钮。
- [x] Shift 框选批量移动（纯函数 selectBuildingsInRect + 逐建筑碰撞校验 + 整组原子提交）。
- [x] Ctrl+C/V 复制粘贴（相对布局、ghost 预览、整组校验 + 套件不足整组回滚、Shift 连放）。
- [x] pointercancel/window 兜底清理框选/移动/粘贴状态，selectTool 切工具清粘贴模式。

## Sprint 17：建造栏与机器检查器对标
- [x] 建造栏分类页签（采集/物流/制造/科研/电力/仓储/工具）+ 数字热键 + 科技锁定 + 套件状态（既有，本 Sprint 复核）。
- [x] P 调试叠层：全部电网连线 + 每段传送带速度/在途件数标识 + 低倍率也显示矿脉余量 + 徽标。
- [x] 建造栏悬停 tooltip：名称/占地/科技授权状态/原料成本（含等级加成）/套件库存。
- [x] 机器检查器：输入就绪条（按类型解析真实配方）+ 输出缓存条 + 件/分速度 + 功耗电网指标。

## Sprint 18：远端行星建厂
- [x] 每行星独立建筑集合/库存(着陆仓储)/地形剖面/矿脉（既有快照系统，本 Sprint 复核）。
- [x] 完成 interstellar-logistics + 一次往返航次后远端行星可落地建厂；着陆仓储 baseHub 作为入口。
- [x] 异星资源（熔火-β 钛/霜环-7 冰）就地建产线；首落提示「先部署风力发电机，再建产线」。
- [x] 星图行星标记显示「N 座设施」（planetFactoryCount，排除着陆仓储）。
- [x] 航线科技化：货运舱容量与航时随物流 MK 等级缩放（MK-V 容量 ×2、航时 −24%），星图同步显示。
- [x] `node qa-harness.cjs playthrough` 全绿（新增 4 条断言）。

## Sprint 19：戴森终局 + 素材补全 + 回归
- [x] 星图戴森可视化：恒星外圈 20 段框架环按节点部署数逐段点亮；完成态（节点满 + 帆/火箭齐备）光环 + 顶部「第一圈戴森框架已点亮 · 恒星能量收集 X/秒」胜利徽标。
- [x] zyuou-gpt-image 补齐 6 张缺失 PNG 并接入 assetPaths：能量/结构/信息矩阵图标、太阳帆发射台、结构火箭发射台、货运舱；均通过 512×512 RGBA + 本地 HTTP 200 校验。
- [x] 发射台建筑接入 image 字段（建造栏/放置预览/Canvas 使用新贴图，程序绘制 fallback 保留）。
- [ ] 手动试玩回归（清单见下）。
- [x] `node qa-browser.cjs` 浏览器级回归覆盖新存档建厂、在途货运舱去返、点击远端星球进入独立地图、远端设施快照、基础存档刷新与 20 段戴森环完成态；输出 `BROWSER_QA_PASS`。

### 手动试玩回归清单（桌面 1280px+，新开无痕页签）
- [ ] 新存档：选职业 → 风力发电 → 采矿→冶炼产线 → 分拣器物流 → 科研站出矩阵。
- [ ] 空格暂停/继续；`=` 升到 10x 再降回；小地图点击跳转相机；`P` 调试叠层开关。
- [ ] Shift 框选多座建筑 → 拖拽整体移动（非法位置整组不移动）；Ctrl+C 复制 → Ctrl+V 粘贴模式连放；Esc 退出。
- [ ] 建造栏悬停看成本/科技 tooltip；点击组装机/工作台切换配方；未研究科技时显示「配方锁定」。
- [ ] 完成星际物流 → 派遣货运舱 → 抵达熔火-β → 星图「进入星球」→ 首落引导 → 部署风机+钛矿产线 → 星图显示设施数。
- [ ] 推进恒星工程：20 轨道节点 + 太阳帆/结构火箭发射台部署组件 → 星图框架环逐段点亮 → 完成态胜利徽标 + 恒星能量收集速率。
- [ ] 刷新页面：科技、库存、航线、行星快照、戴森组件、选中配方全部保留。
- [ ] `prefers-reduced-motion` 开启时：持续旋转/脉冲/进度过渡减弱。
